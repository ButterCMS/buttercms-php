<?php

namespace ButterCMS\Model;

class Author extends Model
{
    protected
        $slug,
        $first_name,
        $last_name,
        $email,
        $bio,
        $title,
        $linkedin_url,
        $facebook_url,
        $pinterest_url,
        $instagram_url,
        $twitter_handle,
        $profile_image;
}
